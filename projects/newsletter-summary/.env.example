# PostgreSQL partagé — à renseigner en env Coolify (jamais commité)
DATABASE_URL=postgresql+asyncpg://admin:CHANGE_ME@shared-postgres:5432/db_newsletter_summary

# COMMS-GATEWAY — l'envoi passe par le gateway (plus de clé Resend dans ce projet)
GATEWAY_URL=http://comms-gateway:8000
GATEWAY_TOKEN=<token scoped fourni par le gateway lors de l'onboarding du client>
RECIPIENT_EMAIL=jean.langlois-meurinne@mailbox.org

# DeepInfra
DEEPINFRA_API_KEY=
DEEPINFRA_MODEL=deepseek-ai/DeepSeek-V4-Flash-0731

# Job matinal
SUMMARY_HOUR=8
SUMMARY_MINUTE=0

# Sécurité webhook inbound
WEBHOOK_TOKEN=
