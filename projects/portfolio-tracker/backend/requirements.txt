fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.0
asyncpg==0.30.0
pydantic-settings==2.6.0
apscheduler==3.10.4
yfinance==1.3.0
feedparser==6.0.11
requests==2.32.3
slack-sdk==3.33.0
aiohttp==3.13.3
redis==5.0.8
# Le référentiel de frameworks est un fichier de DONNÉES INERTES (`app/frameworks/frameworks.yaml`).
# PyYAML était déjà dans l'image comme dépendance transitive de yfinance ; l'épingler
# explicitement, parce qu'un chargeur load-bearing adossé à une transitive casse le jour où la
# transitive s'en va, et l'erreur se lit alors comme un bug du référentiel.
PyYAML==6.0.3
